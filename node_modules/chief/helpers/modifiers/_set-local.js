
const _read = require('./../core/_read');


module.exports = ( file, settings ) => {

	const data = file.split(settings.divider);

	let local = _read.data(data[1]);

	local.markdown = _read.content(data[2]);


	return local;

};
