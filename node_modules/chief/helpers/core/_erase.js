
const fs = require('fs');


module.exports = {

	file : ( path, file, cb ) => {
		return (cb)
			? fs.unlink(path, cb)
			: fs.unlinkSync(path);
	},

	dir : ( path, cb ) => {
		return (cb)
			? fs.rmdir(path, cb)
			: fs.rmdirSync(path);
	}

}