
const fs = require('fs');


module.exports = {

	file : ( path, file, cb ) => {
		return (cb)
			? fs.writeFile(path, file, cb)
			: fs.writeFileSync(path, file);
	},

	dir : ( path, cb ) => {
		return (cb)
			? fs.mkdir(path, cb)
			: fs.mkdirSync(path);
	}

}