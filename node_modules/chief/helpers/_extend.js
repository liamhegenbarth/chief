
module.exports = ( obj, ...args ) => {

	const out = obj || {};

	for ( let i = 0; i < args.length; i++ )
	{
		if ( !args[i] )
		{
			continue;
		}

		for ( let key in args[i] )
		{
			if ( args[i].hasOwnProperty(key) )
			{
				out[key] = args[i][key];
			}
		}
	}

	return out;

};
